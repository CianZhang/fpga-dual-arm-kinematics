# 归档范围与版本选择

整理以比赛提交目录 `代码/CICC0900959_Project/CICC0900959_Project` 的 RTL 和集成工程为基线。两套 `motion_solver` 文件夹已与 `代码/Project` 对应版本比较，内容一致；不据此推断其余文件全部相同。

保留：两套机械臂 RTL、原仿真源码、一个集成 PDS 工程及约束、原 Python 参考脚本、机械模型、报告及精选图片。

排除：OV5640 视觉工程、工具安装包、厂商资料全集、账单、重复项目副本、构建产物、波形数据库、上位机 ZIP、PSD 和海报模板。原完整演示视频约 523 MB，留在原资料中，首版以报告实物图展示；未伪造视频链接。

修改范围：PDS 源码引用重定位；厂商 IP 引用改为本地生成路径；清除旧机器的 ModelSim 路径。RTL 和原测试源码保持不变。新增文档及工具在贡献说明中单独标记。

根据作者确认，只收录本人建模的 SolidWorks 源文件并保留原目录结构。网络 STL 和其他模型格式只列清单，不重新分发。机械贡献为现有方案基础上的结构补充设计，未对无法打开的装配作完整性保证。

源文件追溯：`source-manifest.json`；机械文件总表：`../mechanical/source-inventory.json`；整理校验结果：`organization-check.json`。
